import React from 'react';

import EmailSubscribeForm from '../EmailSubscribeForm';

const EmailSection = () => (
  <section id="subscribe" className="hero">
    <div className="hero-body">
      <div className="container has-text-centered">
        <EmailSubscribeForm />
      </div>
    </div>
  </section>
);

export default EmailSection;
